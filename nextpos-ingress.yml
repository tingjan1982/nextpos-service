apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nextpos-ingress
spec:
  tls:
    - secretName: nextpos-secret-tls2
  backend:
    serviceName: nextpos-service-nodeport
    servicePort: 80     
